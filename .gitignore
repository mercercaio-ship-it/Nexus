# Dependencies
node_modules/

# Build outputs
dist/
build/
out/
coverage/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example

# Credentials, certificates, and key material (belt-and-suspenders;
# CreativEdge does not use these by design, but block them everywhere
# in case anything is ever placed in the tree accidentally)
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.pkcs12
secrets.json
credentials.json
service-account*.json
gcp-key.json
aws-credentials*
.netrc
.npmrc
.yarnrc
.pypirc

# CreativEdge local runtime
.creativedge/
**/.creativedge/
sessions.db
sessions.db-shm
sessions.db-wal
*.db
*.db-shm
*.db-wal

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude / AI-tool local-only state
.claude/settings.local.json
.claude/cache/
.claude/memory/
.cursor/
.aider*
.continue/
.copilot/
GEMINI.md.local
AGENTS.md.local

# Local curl test bodies
body-*.json

# Pre-publication local-only scratch / draft / superseded artifacts
# These files exist on the maintainer's disk but are intentionally
# excluded from the public repo:
#   - chat.md                                — scratch dialogue draft
#   - Logo-Design.md                         — marketplace pricing research
#   - console.html                           — Phase-1 admin console
#                                              prototype (superseded by
#                                              the React frontend/)
#   - creativedge-complete-services-catalog.md
#                                            — product positioning copy
#                                              (out of scope per the
#                                              docs audit trail)
chat.md
Logo-Design.md
console.html
creativedge-complete-services-catalog.md